---
const techPrimary = [
  "Next.js",
  "Astro",
  "React",
  "TypeScript",
  "Node.js",
  "Express.js",
  "Supabase",
  "PostgreSQL",
  "TailwindCSS"
];
const techExtra = [
  "Stripe",
  "MercadoPago",
  "JWT",
  "OAuth",
  "REST APIs",
  "CI/CD",
  "Serverless",
  "SEO"
];
const softSkills = [
  "Technical leadership",
  "End-to-end ownership",
  "Product-oriented mindset",
  "Clear communication (ES/EN)",
  "Pragmatic architecture",
  "Working with cross-functional teams"
];
---

<section id="stack" class="py-10 border-t border-slate-800/60">
  <header class="mb-6">
    <h2 class="text-xl md:text-2xl font-semibold text-slate-50">
      Stack & how I work
    </h2>
    <p class="text-sm text-slate-400 mt-1 max-w-xl">
      Modern, scalable stack focused on delivering value quickly without sacrificing technical quality.
    </p>
  </header>

  <div class="grid md:grid-cols-3 gap-6">
    <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
      <h3 class="text-sm font-semibold text-slate-50 mb-2">Core tech</h3>
      <div class="flex flex-wrap gap-1.5 text-[11px]">
        {techPrimary.map((t) => (
          <span class="rounded-full bg-slate-800 px-2.5 py-1 text-slate-200">
            {t}
          </span>
        ))}
      </div>
    </div>

    <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
      <h3 class="text-sm font-semibold text-slate-50 mb-2">Extras & product</h3>
      <div class="flex flex-wrap gap-1.5 text-[11px]">
        {techExtra.map((t) => (
          <span class="rounded-full bg-slate-800 px-2.5 py-1 text-slate-200">
            {t}
          </span>
        ))}
      </div>
    </div>

    <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
      <h3 class="text-sm font-semibold text-slate-50 mb-2">How I work</h3>
      <ul class="text-xs text-slate-300 space-y-1.5">
        {softSkills.map((s) => <li>• {s}</li>)}
      </ul>
    </div>
  </div>
</section>
